{
  "name": "music-theory-tutor-core",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs",
    "build:site": "npm run build && node scripts/build-site.mjs"
  },
  "devDependencies": {
    "typescript": "5.9.2"
  }
}
